import React from 'react';

export function ChatSidebar(props: any) {
  return (
    <div className="p-4 border rounded-md text-sm text-muted-foreground">
      [Placeholder] ChatSidebar component
    </div>
  );
}

export default ChatSidebar;
